# MQL4_execリポジトリ エージェントガイド

## 目的と適用範囲

このファイルは、このディレクトリとその配下で行うMT4バックテスト、パラメータ最適化、検証、レポート取得、および機械的なデータ変換に適用します。

このリポジトリは、統括プロジェクト `C:\Users\yutom\Projects\EABuilder` が管理する外部プロジェクトです。ストラテジーの仕様、実験計画、結果の解釈、採否判断、およびカスタムエージェント定義はEABuilder側で管理します。

## 統括文書の読込み

すべてのタスクで、作業を開始する前に以下の文書を全文読みます。

1. `C:\Users\yutom\Projects\EABuilder\PROJECT_MAP.md`
2. `C:\Users\yutom\Projects\EABuilder\AGENTS.md`
3. 対象ファイルまでの経路に、より近い `AGENTS.md` または `AGENTS.override.md` が存在する場合は、その文書

外部プロジェクトの場所と役割については `PROJECT_MAP.md` を現在値の正本とし、プロジェクト横断の作業規則と安全上の制約についてはEABuilderの `AGENTS.md` に従います。このファイルはMQL4_exec固有の規則を追加します。

文書間、設定値、実際のパス、またはユーザーの依頼内容に矛盾がある場合は、推測で実行せず、不一致をユーザーへ報告して解消します。

## 責務の境界

このリポジトリでは、以下の決定論的な処理を行います。

- MT4テスター入力ファイルの生成
- バックテスト、最適化、および検証の開始
- MT4プロセスの完了とレポート生成の確認
- 生レポート、実行条件、および実行状態の保存
- 保存した生レポートからCSV、JSON、またはExcelへの再現可能な機械的変換

このリポジトリでは、以下を行いません。

- MQL4ソース、共有インクルード、カスタムインジケーター、または `.ex4` の編集
- ストラテジーの採否、収益性の最終評価、または自律的な売買ロジック変更
- バックテスト結果に応じてEAを自動的に書き換える処理の追加
- ライブ取引ターミナルの起動、AutoTradingの有効化、または注文送信

結果の定量評価と改善判断は、EABuilderのメインエージェントおよび `quant_analyst` が保存済み成果物を使用して行います。

## リポジトリ構成

- `Backtest\Backtest.py`：通常バックテストを実行し、生HTML、正規化済みCSV、検証JSON、および定型集計Excelをrun単位で生成します。
- `Backtest\config.ini`：EAごとのテストモデル、時間足、および通貨ペアを定義します。
- `Optimization\Optimization.py`：期間分割したパラメータ最適化を実行し、集計Excelを生成します。
- `Optimization\config.ini`：最適化対象、期間、および分割月数を定義します。
- `Validation\Validation.py`：Tracker出力を使ったエッジおよび値動き検証を実行します。
- `Validation\Move_Validation.py`：旧式または移行途中の検証スクリプトとして扱い、ユーザーが明示的に指定しない限り実行しません。
- `utils\set_inputfile.py`：MT4テスター入力ファイルを生成します。
- `utils\STReportReader.py`：MT4のHTMLレポートを機械的に解析します。
- `utils\setting.conf`：MT4テスターディレクトリ、バックテスト用ターミナル、および結果出力先の絶対パスを定義します。
- `results`：生成結果の出力先です。Git管理対象外の生成物として扱い、既存結果を無断で上書きしません。
- `rakutenMT4_log`：ログ保管場所として扱い、必要な範囲だけを読みます。
- `.venv`：現在はPOSIX形式の `bin` を持つ環境です。Windows形式の `Scripts` 環境と互換であると仮定したり、無断で作り直したりしてはいけません。

## Backtest成果物仕様

- 通常バックテストの成果物仕様の正本は `docs\BACKTEST_REPORT_SCHEMA.md` です。
- この仕様は `Backtest\Backtest.py` と通常バックテスト用の `BacktestReport` にだけ適用し、OptimizationおよびValidationには適用しません。
- Optimizationを改修する場合は、Backtest仕様へ追記せず、別の仕様書と独立したschema versionを定義します。
- `Backtest.py`または`STReportReader.py`でBacktest成果物の列、意味、計算式、欠損処理、検証条件、またはディレクトリ構造を変更する場合は、同じタスクで仕様書を更新します。
- 非互換変更ではmajor、後方互換な追加ではminor、意味を変えない修正ではpatchの更新要否を確認します。
- コード、仕様書、保存済みHTMLからの再生成結果が一致することを確認します。
- 従来形式の `Backtest_*.xlsx` と固定名HTMLは後方互換用であり、エージェント評価用成果物の正本として扱いません。

## 現在の実行環境に関する注意

- Pythonの要求バージョンは `pyproject.toml` と `.python-version` により3.11です。依存関係は `pyproject.toml` と `uv.lock` を正本とします。
- 現在の `Backtest\Backtest.py` はWindows絶対パスを `/mnt/<drive>/...` に変換して使用するため、WSL環境を前提としています。
- 現在の `Optimization\Optimization.py` は `wslpath`、`../utils`、および `./config.ini` に依存するため、WSL上で `Optimization` ディレクトリを作業ディレクトリとして実行します。
- 現在の `Validation\Validation.py` は `../utils` と `./config.ini` に依存するため、実行する場合は `Validation` ディレクトリを作業ディレクトリとします。
- 現在の `utils\setting.conf` には `[setting]` セクションだけがあり、`Validation.py` が要求する `[Validation]` セクションはありません。設定が明示的に整備されるまで、値を推測してValidationを実行してはいけません。
- Windows PowerShell、Windows Python、WSL、WSL Python、およびそれぞれの仮想環境を混在させてはいけません。実行前に、対象スクリプトが期待するパス形式とPython環境を確認します。

## 作業開始時の手順

1. タスクの種別がBacktest、Optimization、Validation、レポート再解析、または実行コード修正のどれかを確認します。
2. 対象EA、通貨ペア、時間足、テストモデル、期間、スプレッド前提、初期証拠金、ロットまたはリスク設定、および入力パラメータを確認します。
3. MQL4リポジトリとMQL4_execの両方でGit状態を確認し、既存のユーザー変更を記録します。
4. 対象EAの現在の `.ex4` と、最適化で必要な `tester\<EA名>.set` の存在、更新日時、および可能であればハッシュを確認します。
5. `utils\setting.conf`、対象の `config.ini`、実行スクリプト、および出力先を読み、パスとセクションが実在することを確認します。
6. 同じバックテスト用ターミナルまたは共有テスターディレクトリを使用する別の実行が動作中でないことを確認します。
7. 既存出力と衝突しない一意の実行識別子と保存先を決めます。
8. 実行前の条件、設定ファイル、Git状態、および対象成果物を再現できる形で記録します。

## 共有リソースと直列実行

以下は共有リソースです。

- `C:\Users\yutom\AppData\Roaming\MetaQuotes\Terminal\22B5A58A650C24CEB4A7175EED132ECE\tester\test_params.txt`
- `C:\Users\yutom\AppData\Roaming\MetaQuotes\Terminal\22B5A58A650C24CEB4A7175EED132ECE\tester\tmpdir`
- `C:\Users\yutom\AppData\Roaming\MetaQuotes\Terminal\22B5A58A650C24CEB4A7175EED132ECE\tester\files\_tmp.csv`
- `C:\Program Files (x86)\Rakuten MetaTrader 4 BackTest\terminal.exe`

- バックテスト、最適化、および検証は、完全に分離されたターミナルデータディレクトリと出力先が用意されていない限り、必ず直列に実行します。
- EAのコンパイル、テスト実行、および結果保存は、前工程の完了と成果物確認を待ってから次へ進みます。
- MT4の起動はメインCodexエージェントだけが行い、サブエージェントに実行させてはいけません。
- 別のターミナルプロセスが動作中の場合は、新しい実行を開始しません。
- `test_params.txt`、`tmpdir`、`_tmp.csv`、固定名レポート、および既存Excelが上書きされる可能性を、実行前に確認します。

## 実行規則

- バックテストには `C:\Program Files (x86)\Rakuten MetaTrader 4 BackTest\terminal.exe` だけを使用し、通常取引用の `Rakuten MetaTrader 4\terminal.exe` と混同してはいけません。
- Pythonスクリプトは、その相対パスが前提とする作業ディレクトリから実行します。
- 通常バックテストの引数は、EA名、開始日、終了日の順です。日付は既存スクリプトが期待する `YYYY.MM.DD` 形式を使用します。
- 最適化では、対象EA名と同名の `.set` がテスターディレクトリ直下にあり、最適化対象行が `F=1` として定義されていることを確認します。
- 実行コマンドを組み立てる前に、WindowsパスとWSLパスをそれぞれ明示的に解決し、文字列連結だけで削除・移動対象を決めてはいけません。
- 実行後は、プロセスの終了コード、ターミナル終了、生HTMLレポートの存在とサイズ、解析成功、Excelまたは正規化済み結果の存在、および行数が空でないことを確認します。
- 例外、レポート欠落、空のテーブル、条件不一致、重複実行、または解析失敗を成功として扱ってはいけません。
- `Optimization.py` にはレポート解析例外を無視する処理があるため、結果が一部欠落していないか、期待実行数と取得レポート数を明示的に照合します。

## 生レポートと削除処理

- 集計またはクリーンアップより前に、生HTMLレポート、使用したテスター入力、および実行条件を永続的な出力先へ保存します。
- `Backtest.py` は `tmpdir` を出力先へコピーした後に削除します。コピー成功と出力内容を確認してから、削除成功を受け入れます。
- `Optimization.py` と `Validation.py` は、現在の実装では生レポートを永続保存せずに `tmpdir` を削除する可能性があります。生レポートの保存方法が確立されるまで、そのまま実行してはいけません。
- `shutil.rmtree` などの再帰削除前に、解決済み絶対パスが意図した `tester\tmpdir` であることを確認します。
- 実行元プロセスが完了し、保存済み出力が検証されるまで、共有一時ディレクトリを削除しません。
- 以前の実験結果をユーザーの明示的な承認なしに削除または上書きしてはいけません。

## Pythonおよび設定ファイルの変更規則

- 実行コードの変更は、ユーザーがMQL4_execの修正を依頼した場合だけ行います。バックテスト実行依頼だけを、コード変更の許可として扱ってはいけません。
- `mql4_implementer` にMQL4_execのPython、設定、ロックファイル、または結果を変更させてはいけません。
- 変更は要求を満たす最小限に限定し、既存のユーザー変更を保持します。
- パス処理では、WindowsパスとWSL/POSIXパスの境界を明示し、可能な範囲で `pathlib` と `PureWindowsPath` を使用します。
- 新しい設定値の解釈に `eval` を追加してはいけません。既存の `eval` を変更対象とする場合は、安全な解析方法を使用します。
- 例外を無条件に握りつぶす処理を追加してはいけません。失敗したEA、期間、通貨ペア、時間足、およびレポートパスを記録します。
- `pyproject.toml` または `uv.lock` は、依存関係変更が明示的に必要な場合だけ更新し、両者の整合を保ちます。
- 実行スクリプトはトップレベルでMT4を起動するため、構文確認の目的で安易にimportしてはいけません。実処理を起動しない方法で構文を確認します。
- 実行動作を変更した場合は、小さな非破壊的確認を行った後、承認済み条件で実際のMT4実行を確認します。

## 結果の受け渡し

重要な実行ごとに、取得可能な範囲で以下を保存または報告します。

- 一意の実行識別子
- EA名とストラテジーバージョン
- MQL4とMQL4_execのGitコミットおよび作業ツリー状態
- EAソースまたは `.ex4` のハッシュ
- 使用した設定ファイルと入力パラメータ
- 通貨ペア、時間足、テストモデル、期間、およびスプレッド前提
- 初期証拠金、ロットまたはリスク設定
- 最適化範囲、ステップ、および試行予定数
- 生レポート、正規化済みデータ、およびExcelの絶対パス
- 期待実行数、成功数、失敗数、およびエラー内容

MQL4_execは機械的な取得・変換結果をEABuilderへ渡します。ストラテジーの採用、棄却、および改善内容を自動決定してはいけません。

## ライブ取引の安全性

- バックテスト実行はライブ取引を許可するものではありません。
- ユーザーの明示的な承認なしに、通常取引用ターミナルを起動する、AutoTradingを有効化する、ライブチャートへEAを適用する、口座設定を変更する、または注文を送信してはいけません。
- アカウント認証情報、トークン、および個人設定を読み取ったり出力したりしてはいけません。

## エージェントの連携

- 要件、実験計画、MT4起動、結果の採否、および最終回答はEABuilderのメインCodexエージェントが管理します。
- `quant_analyst` は、実行完了後に保存済みレポートと正規化済み結果を読み取り専用で評価します。
- ストラテジー批評、MQL4レビュー、定量分析など、独立した読み取り専用作業だけを必要に応じて並列化します。
- MT4実行、共有一時ファイル操作、結果保存、およびクリーンアップをサブエージェントへ委任してはいけません。
- EABuilderのカスタムエージェントを使用するタスクは、EABuilderをプロジェクトとして開始します。このリポジトリへ別のエージェント定義を複製しません。

## Gitと変更管理

- 作業前後にGit状態を確認し、既存のユーザー変更と生成結果を保持します。
- 関係のない変更を破棄、リセット、上書き、ステージング、または取り込んではいけません。
- ユーザーが明示的に依頼しない限り、コミット、プッシュ、タグ付け、ブランチ操作、または履歴書換えを行いません。
- `results`、`.venv`、`__pycache__`、ログ、認証情報、および一時ファイルをGitへ追加してはいけません。

## 完了条件

実行またはMQL4_execの変更は、以下の該当項目を満たした場合にのみ完了とします。

- 対象EA、条件、実行環境、および入力ファイルが特定されていること。
- 実行前後のGit状態と対象成果物が記録されていること。
- MT4プロセスが終了し、空ではない生レポートが保存されていること。
- 正規化済みデータまたはExcelを、生レポートから再現できること。
- 期待実行数と成功・失敗件数が照合されていること。
- 既存結果とユーザー変更が保持されていること。
- 使用条件、出力パス、エラー、未確認事項、および次に必要な評価がメインエージェントへ報告されていること。
